# TravelLine & Bnovo ID Finder

Скрипт для Tampermonkey, который автоматически находит и отображает идентификаторы отелей TravelLine и Bnovo на любой странице.

## 📋 Описание

**TravelLine & Bnovo ID Finder** - это инструмент, который перехватывает сетевые запросы к сервисам TravelLine и Bnovo, извлекает ID отелей и отображает их в удобном виде в правом верхнем углу страницы. Скрипт также предоставляет быстрые ссылки для перехода к отелю в HotelLab и ReservationSteps.

## ✨ Возможности

- 🔍 **Автоматическое обнаружение** - находит ID отелей из сетевых запросов
- 📊 **Поддержка двух провайдеров**:
  - **TravelLine** - извлекает hotel code и alias (если доступен)
  - **Bnovo** - извлекает UUID отеля
- 🎯 **Визуальное отображение** - показывает ID в фиксированном блоке в правом верхнем углу
- 🔗 **Быстрые ссылки**:
  - **HL** - переход к отелю в HotelLab Admin
  - **RS** - переход к отелю в ReservationSteps (только для Bnovo)
- 📝 **Определение alias** - автоматически находит alias TravelLine из скриптов страницы

## 🚀 Установка

### Шаг 1: Установка Tampermonkey

1. Установите расширение [Tampermonkey](https://www.tampermonkey.net/)
2. Откройте Tampermonkey Dashboard

### Шаг 2: Установка скрипта

1. Откройте файл `tl_bnovo_finder.js`
2. Скопируйте весь код
3. В Tampermonkey Dashboard создайте новый скрипт
4. Вставьте код и сохраните (Ctrl+S)

## 🔧 Как работает

### Перехват запросов

Скрипт перехватывает сетевые запросы тремя способами:

1. **Fetch API** - перехватывает все `fetch()` запросы
2. **XMLHttpRequest** - перехватывает XHR запросы
3. **Performance API** - проверяет уже загруженные ресурсы

### Извлечение ID

#### TravelLine
- Ищет запросы к доменам `*.tlintegration.ru`
- Извлекает параметры `hotel` или `hotelcode` из URL
- Пытается найти alias (например, `TL-INT-cosmosgroup`) из скриптов страницы
- Отображает: `TL-INT-cosmosgroup_12041` или `travelline_12041`

#### Bnovo
- Ищет запросы к доменам `*.reservationsteps.ru`
- Извлекает параметр `uid` из URL (UUID формат)
- Отображает: `bnovo_17308a7d-cd35-4ec4-8b2f-c96e646720f4`

### Отображение

Найденный ID отображается в фиксированном блоке в правом верхнем углу страницы:
- Темный полупрозрачный фон
- Белый текст с ID
- Кнопки быстрого доступа (HL, RS)

## 📖 Примеры использования

### TravelLine отель
При посещении сайта с интеграцией TravelLine:
- Скрипт находит ID из запросов к `tlintegration.ru`
- Отображает: `TL-INT-cosmosgroup_12041`
- Предоставляет кнопку **HL** для перехода в HotelLab

### Bnovo отель
При посещении сайта с интеграцией Bnovo:
- Скрипт находит UUID из запросов к `reservationsteps.ru`
- Отображает: `bnovo_17308a7d-cd35-4ec4-8b2f-c96e646720f4`
- Предоставляет кнопки **HL** и **RS**

## ⚙️ Технические детали

### Совместимость
- Работает на любых сайтах (`@match *://*/*`)
- Не требует специальных разрешений (только `GM_addStyle`)

### Производительность
- Минимальное влияние на производительность
- Перехват происходит асинхронно
- Дополнительные проверки с задержкой для ресурсов, загружающихся после `document-idle`

### Обновления
Скрипт автоматически проверяет обновления через Tampermonkey при наличии `@updateURL`.

## 🐛 Устранение проблем

### ID не отображается
1. Убедитесь, что на странице есть запросы к TravelLine или Bnovo
2. Откройте консоль браузера (F12) и проверьте сообщения `[ID Finder]`
3. Проверьте, что скрипт активен в Tampermonkey Dashboard

### Неправильный ID
- Скрипт использует первый найденный валидный ID
- Если на странице несколько отелей, может отображаться ID первого найденного

## 📝 Версия

Текущая версия: **0.6**

## 👤 Автор

**Mr Vi**

## 📄 Лицензия

См. файл [LICENSE](../LICENSE) в корне репозитория.

